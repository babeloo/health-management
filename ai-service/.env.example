# JWT 配置（必须与 NestJS 后端保持一致）
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRES_IN=86400

# DeepSeek API 配置
DEEPSEEK_API_KEY=your-deepseek-api-key
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_TIMEOUT=60
DEEPSEEK_MAX_RETRIES=3
DEEPSEEK_MODEL=deepseek-chat

# Qdrant 向量数据库
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=health_knowledge
RAG_TOP_K=3
RAG_SCORE_THRESHOLD=0.7

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis123
REDIS_DB=0
REDIS_CACHE_TTL=3600

# MongoDB 配置
MONGODB_URL=mongodb://admin:mongo123@localhost:27017/health_messages?authSource=admin
MONGODB_DB_NAME=health_messages

# AI 免责声明
DISCLAIMER_TEXT=此建议仅供参考，请咨询专业医生。AI 生成内容不应替代专业医疗诊断和治疗。

# 环境配置（development, staging, production）
ENVIRONMENT=development

# CORS（逗号分隔域名列表，或使用 "*"）
# 生产环境必须配置具体域名，不能使用 "*"
CORS_ORIGINS=*
